/** This class represents a translatable message to the user. */
public class Message {
  // different constructors: plain message, message with one parameter,
  // message with multiple parameters. (That is, replaceables.)

  /** This method translates the message and does any necessary
      replacement of parameters into the string itself. */
  public String toString() {}
}
